TRAFFIC_HTML = r'''<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Personal CIC // Traffic</title>
<link rel="stylesheet" href="/static/maplibre/maplibre-gl.css">
<script src="/static/maplibre/maplibre-gl.js"></script>
<style>
:root{color-scheme:dark;--bg:#080c10;--panel:#0f151b;--panel2:#121b22;--line:#25323d;--text:#e7edf2;--muted:#9aabba;--good:#74d99f;--warn:#e8c56d;--bad:#ef7e7e;--accent:#79b8ff;--crowd:#c99cff;--city:#79d4c5;--cmpd:#f0ad79;--tomtom:#c99cff;--flow:#70d6c7;--ui-generation:1;--fs-xs:12px;--fs-sm:13px;--fs-md:15px;--fs-lg:18px;--fs-xl:22px;--lh:1.5}
*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;font-size:var(--fs-md);line-height:var(--lh)}header{position:sticky;top:0;z-index:20;background:rgba(8,12,16,.97);border-bottom:1px solid var(--line)}.brand{display:flex;align-items:baseline;gap:14px;padding:18px 24px 12px}.brand h1{font-size:var(--fs-xl);letter-spacing:.10em;line-height:1.2;margin:0}.chip{font-size:var(--fs-xs);padding:3px 9px;border:1px solid var(--line);border-radius:999px;color:var(--muted)}nav{display:flex;gap:10px;padding:0 24px 14px;overflow-x:auto}nav a,nav span{color:var(--muted);text-decoration:none;border:1px solid var(--line);padding:7px 11px;border-radius:5px;font-size:var(--fs-sm);line-height:1.25;white-space:nowrap}.future{opacity:.45}.active{color:var(--text)!important;border-color:#466174!important;background:#111b23}a:focus-visible,button:focus-visible,input:focus-visible{outline:2px solid var(--accent);outline-offset:2px}.status-strip{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px;padding:14px 24px}.status,.panel{border:1px solid var(--line);background:var(--panel);border-radius:8px}.status{padding:12px 14px}.label{font-size:var(--fs-xs);letter-spacing:.08em;line-height:1.3;color:var(--muted)}.value{font-size:var(--fs-lg);margin-top:5px;line-height:1.25}.current,.nominal{color:var(--good)}.degraded{color:var(--warn)}.unavailable{color:var(--bad)}main{padding:0 24px 30px}.layout{display:grid;grid-template-columns:minmax(0,2.2fr) minmax(360px,.9fr);gap:14px}.panel h2{margin:0;padding:13px 15px;border-bottom:1px solid var(--line);background:var(--panel2);font-size:var(--fs-sm);letter-spacing:.07em;line-height:1.35}.map-tools{display:flex;gap:8px;flex-wrap:wrap;padding:10px 12px;border-bottom:1px solid var(--line)}button{font:inherit;font-size:var(--fs-xs);line-height:1.25;min-height:34px;color:var(--text);background:#111a21;border:1px solid var(--line);border-radius:5px;padding:7px 10px;cursor:pointer}button:hover{border-color:#466174}.layers{display:flex;gap:10px 14px;flex-wrap:wrap;margin-left:auto}.layers label{font-size:var(--fs-xs);line-height:1.3;color:var(--muted);display:flex;gap:6px;align-items:center;min-height:30px}.map-wrap{position:relative;overflow:hidden;background:#080d11;height:clamp(520px,62vh,780px);min-height:520px;overscroll-behavior:none;touch-action:none;isolation:isolate}.map-engine{position:absolute;inset:0;z-index:0}.map-overlay{position:absolute;inset:0;z-index:2;width:100%;height:100%;pointer-events:none}.map-note{position:absolute;z-index:4;left:10px;bottom:9px;background:rgba(8,12,16,.88);border:1px solid var(--line);padding:7px 9px;font-size:var(--fs-xs);line-height:1.4;color:var(--muted);max-width:76%;pointer-events:none}.map-mode-note{color:var(--warn)}.maplibregl-ctrl-group{background:#101820!important;border:1px solid var(--line)}.maplibregl-ctrl button{filter:invert(.88)}.maplibregl-ctrl-attrib{background:rgba(8,12,16,.82)!important;color:var(--muted)!important}.maplibregl-ctrl-attrib a{color:var(--muted)!important}.side{display:flex;flex-direction:column;gap:14px}.source-list,.gap-list,.event-list,.detail{padding:11px 13px}.source{padding:9px 0;border-bottom:1px solid #1b2730}.source:last-child{border-bottom:0}.source-head{display:flex;justify-content:space-between;gap:10px;font-size:var(--fs-sm);line-height:1.35}.source-sub{font-size:var(--fs-xs);color:var(--muted);margin-top:4px;line-height:1.5}.gap{font-size:var(--fs-xs);color:var(--warn);padding:8px 0;border-bottom:1px solid #1b2730;line-height:1.5}.detail{font-size:var(--fs-sm);line-height:1.55;min-height:132px;overflow-wrap:anywhere}.detail strong{color:var(--accent)}.event-list{max-height:380px;overflow:auto}.event-row{font-size:var(--fs-xs);padding:10px 0;border-bottom:1px solid #1b2730;cursor:pointer}.event-row:hover{color:var(--accent)}.event-row .meta{color:var(--muted);margin-top:4px;line-height:1.45}.event-row .headline{font-size:var(--fs-sm);line-height:1.4}.badge{display:inline-block;border:1px solid #33444f;border-radius:3px;padding:1px 4px;margin-right:4px;font-size:var(--fs-xs);color:var(--muted)}.badge.hot{color:var(--warn);border-color:#6a5831}.badge.critical{color:var(--bad);border-color:#6d3939}.intel{padding:11px 13px}.intel-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.intel-metric{border:1px solid #1b2730;background:#0b1116;padding:9px}.intel-metric .n{display:block;font-size:17px;margin-top:4px}.intel-note{margin-top:9px;color:var(--muted);font-size:var(--fs-xs);line-height:1.5}.intel-list{margin-top:9px;border-top:1px solid #1b2730}.intel-row{padding:8px 0;border-bottom:1px solid #1b2730;font-size:var(--fs-xs);line-height:1.4;cursor:pointer}.intel-row:hover{color:var(--accent)}.legend{display:flex;gap:10px 14px;flex-wrap:wrap;padding:9px 12px;border-top:1px solid var(--line);font-size:var(--fs-xs);line-height:1.4;color:var(--muted)}.sw{display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:3px}.external{margin-top:10px}.external-body{padding:13px}.external iframe{width:100%;height:520px;border:1px solid var(--line);border-radius:4px;background:#0a1015}.disclosure{font-size:var(--fs-xs);color:var(--warn);line-height:1.55;margin-bottom:10px}.foot{font-size:var(--fs-xs);line-height:1.55;color:var(--muted);padding:12px 24px 24px}.roads{fill:none;stroke:#263640;stroke-width:1.1;vector-effect:non-scaling-stroke}.secondary{stroke:#18242c;stroke-width:.55}.county{fill:none;stroke:#3a4d59;stroke-width:1;vector-effect:non-scaling-stroke}.place{fill:#8498a7;font-size:12px}.evt{fill:#ef7e7e;stroke:#0a0e12;stroke-width:1.5;vector-effect:non-scaling-stroke;cursor:pointer}.evt-wzdx{fill:#e8c56d}.evt-cmpd{fill:var(--cmpd)}.evt-city{fill:var(--city)}.evt-tomtom{fill:var(--tomtom)}.camera{fill:#79b8ff;stroke:#0a0e12;stroke-width:1.2;cursor:pointer}.sign{fill:#e8c56d;stroke:#0a0e12;stroke-width:1.2;cursor:pointer}.site-anchor{fill:#fff;stroke:#79b8ff;stroke-width:2;vector-effect:non-scaling-stroke}.site-label{fill:#d9f2ff;font-size:12px;paint-order:stroke;stroke:#080d11;stroke-width:3px;stroke-linejoin:round;pointer-events:none}.event-line{fill:none;stroke:#ef7e7e;stroke-width:2;vector-effect:non-scaling-stroke;opacity:.75}.line-wzdx{stroke:#e8c56d}.line-city{stroke:var(--city)}.line-tomtom{stroke:var(--tomtom)}.flow-line{fill:none;stroke:var(--flow);stroke-width:3;vector-effect:non-scaling-stroke;opacity:.88;cursor:pointer}.flow-slow{stroke:#e8c56d}.flow-congested{stroke:#ef9f6d}.flow-severe,.flow-closed{stroke:#ef7e7e}.flow-label,.road-label{fill:#9db0bd;font-size:var(--fs-xs);paint-order:stroke;stroke:#080d11;stroke-width:3px;stroke-linejoin:round;pointer-events:none}.road-label{font-size:var(--fs-xs);fill:#788e9e}.impact-closure{stroke-width:3.3}.impact-delay{stroke-width:2.7}.event-symbol{stroke:#080d11;stroke-width:1.4;vector-effect:non-scaling-stroke;cursor:pointer}@media(max-width:1250px){.status-strip{grid-template-columns:repeat(3,minmax(0,1fr))}.layout{grid-template-columns:minmax(0,1.8fr) minmax(340px,1fr)}}@media(max-width:1000px){.layout{grid-template-columns:1fr}.status-strip{grid-template-columns:repeat(2,minmax(0,1fr))}.map-wrap{min-height:460px;height:58vh}}@media(max-width:620px){.brand,nav,.status-strip,main,.foot{padding-left:14px;padding-right:14px}.brand{align-items:flex-start;flex-direction:column;gap:8px}.status-strip{grid-template-columns:repeat(2,minmax(0,1fr))}.map-tools{align-items:stretch}.layers{margin-left:0}.intel-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}
</style>
</head>
<body>
<header><div class="brand"><h1>PERSONAL CIC // TRAFFIC</h1><span class="chip">READ-ONLY</span><span class="chip">WORLDSTATE</span></div><nav><a href="/systems">SYSTEMS</a><a href="/world">WORLD</a><a class="active" href="/traffic">TRAFFIC</a><span class="future">HOUSE</span><span class="future">SENSORS</span><span class="future">SYSTEM / AI</span></nav></header>
<section class="status-strip">
<div class="status"><div class="label">TRAFFIC PICTURE</div><div id="picture" class="value unavailable">--</div></div>
<div class="status"><div class="label">EVENT KERNELS</div><div id="kernels" class="value">--</div></div>
<div class="status"><div class="label">CAMERAS</div><div id="cams" class="value">--</div></div>
<div class="status"><div class="label">ACTIVE SIGNS</div><div id="signs" class="value">--</div></div>
<div class="status"><div class="label">FLOW PROBES</div><div id="flows" class="value">--</div></div>
<div class="status"><div class="label">API</div><div id="api" class="value unavailable">CONNECTING</div></div>
</section>
<main>
<div class="layout">
<section class="panel">
<h2>LOCAL MOBILITY PICTURE // SOURCE-PRESERVING MAP</h2>
<div class="map-tools"><button id="scope-view" type="button">75 MI SCOPE</button><button id="local-view" type="button">SITE 15 MI</button><button id="fit-active" type="button">FIT ACTIVE</button><button id="zoom-in" type="button">ZOOM +</button><button id="zoom-out" type="button">ZOOM −</button><button id="rotate-left" type="button">ROTATE ⟲</button><button id="rotate-right" type="button">ROTATE ⟳</button><button id="pitch-up" type="button">PITCH +</button><button id="pitch-down" type="button">PITCH −</button><button id="north-view" type="button">2D NORTH</button><div class="layers">
<label><input id="l-events" type="checkbox" checked> NCDOT EVENTS</label>
<label><input id="l-wzdx" type="checkbox" checked> WZDx</label>
<label><input id="l-city" type="checkbox" checked> CITY</label>
<label><input id="l-cmpd" type="checkbox" checked> CMPD</label>
<label><input id="l-tomtom" type="checkbox" checked> TOMTOM INCIDENTS</label>
<label><input id="l-flow" type="checkbox" checked> TOMTOM FLOW</label>
<label><input id="l-cameras" type="checkbox" checked> CAMERAS</label>
<label><input id="l-signs" type="checkbox" checked> SIGNS</label>
<label><input id="l-viewport-list" type="checkbox" checked> VIEWPORT LIST</label>
<label><input id="l-detail-base" type="checkbox" checked> DETAIL BASE</label>
<label><input id="l-lastknown" type="checkbox"> LAST-KNOWN</label>
</div></div>
<div id="map-wrap" class="map-wrap"><div id="map-engine" class="map-engine" aria-label="Interactive local traffic situation map"></div><svg id="map-overlay" class="map-overlay" role="img" aria-label="CIC traffic evidence overlay"></svg><div id="map-note" class="map-note">Awaiting local map context.</div></div>
<div class="legend"><span><i class="sw" style="background:#ef7e7e"></i>NCDOT event</span><span><i class="sw" style="background:#e8c56d"></i>WZDx work zone / sign</span><span><i class="sw" style="background:#79d4c5"></i>Charlotte closure</span><span><i class="sw" style="background:#f0ad79"></i>CMPD CAD (list until geocoded)</span><span><i class="sw" style="background:#c99cff"></i>TomTom incident</span><span><i class="sw" style="background:#70d6c7"></i>TomTom matched flow segment</span><span><i class="sw" style="background:#79b8ff"></i>camera</span></div>
</section>
<div class="side">
<section class="panel"><h2>COLLECTION // SOURCE HEALTH</h2><div id="sources" class="source-list"></div></section>
<section class="panel"><h2>VIEWPORT // OPERATIONAL PICTURE</h2><div id="viewport-intel" class="intel">Awaiting viewport evidence.</div></section>
<section class="panel"><h2>SELECTED EVIDENCE</h2><div id="detail" class="detail">Select a mapped object or source event. Source identity is preserved; event kernels do not erase underlying observations.</div></section>
<section class="panel"><h2>EVENT OBSERVATIONS</h2><div id="event-list" class="event-list"></div></section>
<section class="panel"><h2>COLLECTION GAPS</h2><div id="gaps" class="gap-list"></div></section>
</div>
</div>
<section class="panel external"><h2>WAZE // EXTERNAL CROWD VISUAL</h2><div class="external-body"><div id="waze-disclosure" class="disclosure">Not loaded. This is intentionally outside canonical CIC WorldState until a supported machine-readable crowd feed is available. Loading it creates direct browser egress to Waze.</div><button id="load-waze" type="button">LOAD WAZE LIVE MAP</button><div id="waze-holder" style="margin-top:8px"></div></div></section>
</main>
<div class="foot">CIC map objects are projections of locally held WorldState. The CIC SITE marker is a fixed operator-context anchor from configured county-GIS evidence; it is not a live operator-position observation and it does not move the traffic collection scope. MapLibre GL JS is a locally served rendering engine; the detailed OpenStreetMap raster layer remains separately sourced browser-direct reference context only and is not canonical WorldState or evidence. Perspective/rotation are projection controls, not new world evidence. Waze is separately labeled operator-opt-in external visual evidence and is never counted as normalized corroboration in RC2. TomTom incident and flow observations remain separately classified and do not establish source independence by themselves. Viewport operational ranking is a presentation aid, not a confidence score or cross-lineage corroboration claim. <span id="generated"></span></div>
<script>
const $=id=>document.getElementById(id);const NS='http://www.w3.org/2000/svg';const VIEW_KEY='personal-cic.traffic.camera.v2';let data=null,context=null,scopeBounds=null,geoView=null,map=null,mapReady=false,mapInitialized=false,scopeMinZoom=null,drawQueued=false;
function state(el,v){v=String(v||'unavailable').toLowerCase();el.textContent=v.toUpperCase();el.className='value '+v}
function age(v){return Number.isFinite(v)?Math.round(v)+'s':'--'}
function node(tag,attrs={}){const e=document.createElementNS(NS,tag);for(const[k,v]of Object.entries(attrs))e.setAttribute(k,String(v));return e}
function radiusBounds(lat,lon,miles){const dlat=miles/69,dlon=miles/(69*Math.max(.2,Math.cos(lat*Math.PI/180)));return[lon-dlon,lat-dlat,lon+dlon,lat+dlat]}
function normalizeBounds(b){if(!b||b.length!==4)return null;const n=b.map(Number);return n.every(Number.isFinite)&&n[0]<n[2]&&n[1]<n[3]?n:null}
function viewCenter(v=geoView){return v?[(v[0]+v[2])/2,(v[1]+v[3])/2]:[null,null]}
function boundsClose(a,b,eps=1e-9){return!!a&&!!b&&a.length===4&&b.length===4&&a.every((v,i)=>Math.abs(v-b[i])<=eps)}
function cameraSnapshot(){if(!map)return null;const c=map.getCenter();return{center:[c.lng,c.lat],zoom:map.getZoom(),bearing:map.getBearing(),pitch:map.getPitch()}}
function saveCamera(){const c=cameraSnapshot();if(!c)return;try{sessionStorage.setItem(VIEW_KEY,JSON.stringify(c))}catch(_e){}}
function savedCamera(){try{const c=JSON.parse(sessionStorage.getItem(VIEW_KEY)||'null');if(!c||!Array.isArray(c.center)||c.center.length!==2)return null;const nums=[...c.center,c.zoom,c.bearing,c.pitch].map(Number);if(!nums.every(Number.isFinite))return null;return{center:[nums[0],nums[1]],zoom:nums[2],bearing:nums[3],pitch:nums[4]}}catch(_e){return null}}
function syncGeoView(){if(!map)return null;const b=map.getBounds();geoView=[b.getWest(),b.getSouth(),b.getEast(),b.getNorth()];return geoView}
function inView(lon,lat){return!!geoView&&lon>=geoView[0]&&lon<=geoView[2]&&lat>=geoView[1]&&lat<=geoView[3]}
function project(lon,lat){if(!mapReady||!Number.isFinite(lon)||!Number.isFinite(lat))return null;const p=map.project([lon,lat]);return[p.x,p.y]}
function linePath(points){return points.map((p,i)=>(i?'L':'M')+p[0].toFixed(1)+' '+p[1].toFixed(1)).join(' ')}
function geographicPoints(g){return(g||[]).map(p=>[Number(p.longitude),Number(p.latitude)]).filter(p=>p.every(Number.isFinite))}
function geometryPoints(g){return geographicPoints(g).map(p=>project(p[0],p[1])).filter(Boolean)}
function drawGeo(group,geometry,klass){if(!geometry)return;const kind=geometry.type,c=geometry.coordinates;const path=pairs=>{const pts=(pairs||[]).map(p=>project(Number(p[0]),Number(p[1]))).filter(Boolean);if(pts.length>1)group.appendChild(node('path',{d:linePath(pts),class:klass}))};if(kind==='LineString')path(c);else if(kind==='MultiLineString')for(const l of c||[])path(l);else if(kind==='Polygon')for(const r of c||[])path(r);else if(kind==='MultiPolygon')for(const p of c||[])for(const r of p||[])path(r)}
function clearMap(){const svg=$('map-overlay');while(svg.firstChild)svg.removeChild(svg.firstChild)}
function setOverlayViewBox(){const svg=$('map-overlay'),rect=$('map-wrap').getBoundingClientRect(),w=Math.max(1,rect.width),h=Math.max(1,rect.height);svg.setAttribute('viewBox',`0 0 ${w} ${h}`);svg.setAttribute('preserveAspectRatio','none')}
function mapStyle(){return{version:8,sources:{osm:{type:'raster',tiles:['https://tile.openstreetmap.org/{z}/{x}/{y}.png'],tileSize:256,maxzoom:19,attribution:'© OpenStreetMap contributors'}},layers:[{id:'background',type:'background',paint:{'background-color':'#080d11'}},{id:'osm-detail',type:'raster',source:'osm',paint:{'raster-opacity':1}}]}}
function applyDetailVisibility(){if(!mapReady||!map.getLayer('osm-detail'))return;map.setLayoutProperty('osm-detail','visibility',$('l-detail-base')?.checked===false?'none':'visible')}
function fitMapBounds(b,{padding=24,maxZoom=null,duration=0}={}){if(!map||!normalizeBounds(b))return;const opts={padding,duration};if(Number.isFinite(maxZoom))opts.maxZoom=maxZoom;map.fitBounds([[b[0],b[1]],[b[2],b[3]]],opts)}
function updateScopeCamera(){if(!map||!scopeBounds)return;map.setMaxBounds([[scopeBounds[0],scopeBounds[1]],[scopeBounds[2],scopeBounds[3]]]);const cam=map.cameraForBounds([[scopeBounds[0],scopeBounds[1]],[scopeBounds[2],scopeBounds[3]]],{padding:18,bearing:0,pitch:0});if(cam&&Number.isFinite(cam.zoom)){scopeMinZoom=cam.zoom;map.setMinZoom(scopeMinZoom)}}
function scheduleDraw(withList=false){if(drawQueued)return;drawQueued=true;requestAnimationFrame(()=>{drawQueued=false;syncGeoView();draw();if(withList)populate()})}
function initMapEngine(){if(map||typeof maplibregl==='undefined'){if(typeof maplibregl==='undefined')$('map-note').textContent='MAP ENGINE UNAVAILABLE // local MapLibre asset missing';return}map=new maplibregl.Map({container:'map-engine',style:mapStyle(),center:[-80.6692,35.0768],zoom:8,bearing:0,pitch:0,maxPitch:75,dragRotate:true,pitchWithRotate:true,touchPitch:true,touchZoomRotate:true,scrollZoom:true,attributionControl:false,hash:false,trackResize:true});map.addControl(new maplibregl.NavigationControl({showZoom:true,showCompass:true,visualizePitch:true}),'top-right');map.addControl(new maplibregl.FullscreenControl(),'top-right');map.addControl(new maplibregl.AttributionControl({compact:true}),'bottom-right');map.on('load',()=>{mapReady=true;const wheelSurface=map.getCanvasContainer();wheelSurface.addEventListener('wheel',e=>{e.preventDefault();e.stopPropagation()},{passive:false});applyDetailVisibility();if(scopeBounds)updateScopeCamera();if(!mapInitialized){const saved=savedCamera();if(saved){map.jumpTo(saved);mapInitialized=true}else if(scopeBounds){fitMapBounds(scopeBounds,{padding:18});mapInitialized=true}}syncGeoView();draw();populate()});map.on('move',()=>scheduleDraw(false));map.on('moveend',()=>{syncGeoView();saveCamera();draw();populate()});map.on('rotateend',()=>{saveCamera();updateMapNote()});map.on('pitchend',()=>{saveCamera();updateMapNote()});map.on('resize',()=>scheduleDraw(false));map.on('click',e=>pickMapObject(e.point.x,e.point.y))}
function detail(title,items){const el=$('detail');el.replaceChildren();const h=document.createElement('div');const strong=document.createElement('strong');strong.textContent=String(title);h.appendChild(strong);el.appendChild(h);for(const[k,v]of items){if(v===null||v===undefined||v==='')continue;const d=document.createElement('div');d.textContent=k+': '+(Array.isArray(v)?v.join(' // '):v);el.appendChild(d)}}
function visibleSource(e){return e.source_authoritative_now||$('l-lastknown').checked}
function layerEnabled(key){return key==='drivenc_events'?$('l-events').checked:key==='wzdx'?$('l-wzdx').checked:key==='charlotte_closures'?$('l-city').checked:key==='cmpd'?$('l-cmpd').checked:key==='tomtom_incidents'?$('l-tomtom').checked:true}
function eventInView(e){const pts=geographicPoints(e.geometry);if(!pts.length||!geoView)return false;let w=Infinity,s=Infinity,east=-Infinity,n=-Infinity;for(const[lon,lat]of pts){w=Math.min(w,lon);east=Math.max(east,lon);s=Math.min(s,lat);n=Math.max(n,lat)}return !(east<geoView[0]||w>geoView[2]||n<geoView[1]||s>geoView[3])}
function zoomRatio(){return scopeBounds&&geoView?(scopeBounds[2]-scopeBounds[0])/(geoView[2]-geoView[0]):1}
function viewMiles(){if(!geoView)return[0,0];const lat=(geoView[1]+geoView[3])/2;return[(geoView[2]-geoView[0])*69*Math.cos(lat*Math.PI/180),(geoView[3]-geoView[1])*69]}
function eventKind(e){const t=((e.event_type||'')+' '+(e.event_subtype||'')+' '+(e.description||'')).toLowerCase();if(e.full_closure===true||/\broad closed\b|\ball lanes closed\b/.test(t))return'closure';if(/\bcrash\b|\baccident\b|\bcollision\b/.test(t))return'crash';if(/\bjam\b|\btraffic\b.*\bstopped\b|\bcongestion\b/.test(t))return'jam';if(/\bhazard\b|\bdangerous\b|\bdebris\b|\bobject\b/.test(t))return'hazard';if(/\bwork\b|\bconstruction\b|\bmaintenance\b/.test(t))return'roadwork';return'other'}
function eventAttention(e){let score=0;if(e.full_closure===true)score+=100;const d=Number(e.delay_seconds);if(Number.isFinite(d)&&d>0)score+=Math.min(60,d/30);const k=eventKind(e);score+=k==='crash'?35:k==='hazard'?30:k==='jam'?20:k==='roadwork'?8:0;return score}
function eventBadges(e){const b=[],k=eventKind(e),d=Number(e.delay_seconds);if(e.full_closure===true)b.push(['CLOSED','critical']);else if(k!=='other')b.push([k.toUpperCase(),k==='crash'||k==='hazard'?'hot':'']);if(Number.isFinite(d)&&d>=60)b.push(['+'+Math.round(d/60)+'m','hot']);return b}
function eventTitle(e){return(e.roadway||e.from_location||e.event_type||'event')+' // '+(e.description||e.event_subtype||'')}
function markerNode(e,x,y,r,klass){const k=eventKind(e);let n;if(k==='closure')n=node('rect',{x:x-r,y:y-r,width:r*2,height:r*2,class:klass+' event-symbol'});else if(k==='hazard')n=node('polygon',{points:`${x},${y-r*1.25} ${x-r*1.1},${y+r} ${x+r*1.1},${y+r}`,class:klass+' event-symbol'});else if(k==='jam')n=node('polygon',{points:`${x},${y-r*1.25} ${x-r*1.1},${y} ${x},${y+r*1.25} ${x+r*1.1},${y}`,class:klass+' event-symbol'});else n=node('circle',{cx:x,cy:y,r:r,class:klass+' event-symbol'});const title=node('title');title.textContent=eventTitle(e);n.appendChild(title);return n}
function geoFeatureLabelPoint(f){const g=f?.geometry;if(!g)return null;let pairs=[];if(g.type==='LineString')pairs=g.coordinates||[];else if(g.type==='MultiLineString')pairs=(g.coordinates||[]).flat();const visible=pairs.filter(p=>Array.isArray(p)&&p.length>=2&&inView(Number(p[0]),Number(p[1])));if(!visible.length)return null;const p=visible[Math.floor(visible.length/2)];return[Number(p[0]),Number(p[1])]}

function updateMapNote(counts={events:0,cameras:0,signs:0,flow:0}){if(!geoView||!data||!map)return;const[cx,cy]=viewCenter(),[mw,mh]=viewMiles(),contextState=data.map_context?.context_state||'UNAVAILABLE',bearing=map.getBearing(),pitch=map.getPitch(),zoom=map.getZoom(),site=data.operator_context?.site_anchor,siteText=site?' // SITE '+Number(site.latitude).toFixed(5)+', '+Number(site.longitude).toFixed(5):' // SITE UNCONFIGURED';$('map-note').textContent='VIEW '+cy.toFixed(4)+', '+cx.toFixed(4)+' // '+mw.toFixed(1)+' × '+mh.toFixed(1)+' mi // z'+zoom.toFixed(1)+' // bearing '+bearing.toFixed(0)+'° // pitch '+pitch.toFixed(0)+'° // visible E '+counts.events+' C '+counts.cameras+' S '+counts.signs+' F '+counts.flow+siteText+' // DETAIL '+($('l-detail-base')?.checked?'OSM REF':'OFF')+' // TIGERweb '+contextState+' // pitch/rotation are projection controls only'}
function draw(){clearMap();if(!data||!geoView||!mapReady)return;setOverlayViewBox();const svg=$('map-overlay'),bg=node('g');svg.appendChild(bg);const zr=zoomRatio();if(context&&scopeBounds&&$('l-detail-base')?.checked===false){for(const f of context.counties||[])drawGeo(bg,f.geometry,'county');if(zr>=1.8)for(const f of context.secondary_roads||[])drawGeo(bg,f.geometry,'roads secondary');for(const f of context.primary_roads||[])drawGeo(bg,f.geometry,'roads');if(zr>=1.3)for(const p of context.places||[]){const lon=Number(p.lon),lat=Number(p.lat);if(!inView(lon,lat))continue;const q=project(lon,lat);const t=node('text',{x:q[0]+3,y:q[1]-3,class:'place'});t.textContent=p.name||'';bg.appendChild(t)}if(zr>=3.5){const seen=new Set(),features=[...(context.primary_roads||[]),...(context.secondary_roads||[])];let labels=0;for(const f of features){if(labels>=30||!f.name||seen.has(f.name))continue;const gp=geoFeatureLabelPoint(f);if(!gp)continue;const q=project(gp[0],gp[1]);const t=node('text',{x:q[0]+4,y:q[1]-4,class:'road-label'});t.textContent=f.name;bg.appendChild(t);seen.add(f.name);labels++}}}
const counts={events:0,cameras:0,signs:0,flow:0};const site=data.operator_context?.site_anchor||null;if(site&&Number.isFinite(Number(site.longitude))&&Number.isFinite(Number(site.latitude))&&inView(Number(site.longitude),Number(site.latitude))){const sp=project(Number(site.longitude),Number(site.latitude));svg.appendChild(node('circle',{cx:sp[0],cy:sp[1],r:5,class:'site-anchor'}));if(map.getZoom()>=16){const label=node('text',{x:sp[0]+8,y:sp[1]-8,class:'site-label'});label.textContent=site.label||'CIC SITE';svg.appendChild(label)}}
for(const e of data.events||[]){if(!visibleSource(e)||!layerEnabled(e.source_key))continue;const geo=geographicPoints(e.geometry);if(!geo.length||!eventInView(e))continue;counts.events++;const pts=geometryPoints(e.geometry);if(pts.length>1){const key=e.source_key,impact=e.full_closure===true?' impact-closure':Number(e.delay_seconds)>=120?' impact-delay':'',klass='event-line '+(key==='wzdx'?'line-wzdx':key==='charlotte_closures'?'line-city':key==='tomtom_incidents'?'line-tomtom':'')+impact;const p=node('path',{d:linePath(pts),class:klass});const title=node('title');title.textContent=eventTitle(e);p.appendChild(title);svg.appendChild(p)}const first=geo.find(p=>inView(p[0],p[1]))||geo[0],pt=project(first[0],first[1]),key=e.source_key,klass='evt '+(key==='wzdx'?'evt-wzdx':key==='charlotte_closures'?'evt-city':key==='cmpd'?'evt-cmpd':key==='tomtom_incidents'?'evt-tomtom':''),c=markerNode(e,pt[0],pt[1],zr<1.5?3.5:5,klass);svg.appendChild(c)}
if($('l-flow').checked){for(const f of data.flow?.probes||[]){if(!f.source_authoritative_now&&!$('l-lastknown').checked)continue;const geo=geographicPoints(f.geometry);if(!geo.some(p=>inView(p[0],p[1])))continue;const pts=geometryPoints(f.geometry);if(pts.length<2)continue;counts.flow++;const ratio=Number(f.speed_vs_free_flow);let band='';if(f.road_closure===true)band=' flow-closed';else if(Number.isFinite(ratio)&&ratio<.4)band=' flow-severe';else if(Number.isFinite(ratio)&&ratio<.65)band=' flow-congested';else if(Number.isFinite(ratio)&&ratio<.85)band=' flow-slow';const path=node('path',{d:linePath(pts),class:'flow-line'+band});const title=node('title');title.textContent=(f.label||f.probe_id)+' // '+(f.current_speed_mph??'--')+'/'+(f.free_flow_speed_mph??'--')+' mph';path.appendChild(title);svg.appendChild(path);if(zr>=2.2){const mid=pts[Math.floor(pts.length/2)],t=node('text',{x:mid[0]+5,y:mid[1]-5,class:'flow-label'}),delta=Number(f.travel_time_delta_seconds);t.textContent=(f.current_speed_mph??'--')+'/'+(f.free_flow_speed_mph??'--')+' mph'+(Number.isFinite(delta)&&delta>=60?' +'+Math.round(delta/60)+'m':'');svg.appendChild(t)}}}
if($('l-cameras').checked){for(const c of data.cameras?.cameras||[]){if(!data.cameras.authoritative_now&&!$('l-lastknown').checked)continue;const lon=Number(c.longitude),lat=Number(c.latitude);if(!inView(lon,lat))continue;counts.cameras++;const p=project(lon,lat);svg.appendChild(node('circle',{cx:p[0],cy:p[1],r:3.2,class:'camera'}))}}
if($('l-signs').checked){for(const s of data.message_signs?.signs||[]){if(!data.message_signs.authoritative_now&&!$('l-lastknown').checked)continue;const lon=Number(s.longitude),lat=Number(s.latitude);if(!inView(lon,lat))continue;counts.signs++;const p=project(lon,lat);svg.appendChild(node('rect',{x:p[0]-3,y:p[1]-3,width:6,height:6,class:'sign'}))}}updateMapNote(counts)}
function screenDistance(a,b){const dx=a[0]-b[0],dy=a[1]-b[1];return Math.hypot(dx,dy)}
function pickMapObject(x,y){if(!data||!mapReady)return;let best=null,bestD=14;const consider=(p,kind,obj)=>{if(!p)return;const d=screenDistance([p[0],p[1]],[x,y]);if(d<bestD){bestD=d;best=[kind,obj]}};for(const e of data.events||[]){if(!visibleSource(e)||!layerEnabled(e.source_key)||!eventInView(e))continue;const pts=geometryPoints(e.geometry);if(!pts.length)continue;for(const p of pts)consider(p,'event',e)}if($('l-flow').checked)for(const f of data.flow?.probes||[]){if(!f.source_authoritative_now&&!$('l-lastknown').checked)continue;const pts=geometryPoints(f.geometry);if(!pts.length)continue;consider(pts[Math.floor(pts.length/2)],'flow',f)}if($('l-cameras').checked)for(const c of data.cameras?.cameras||[]){if(!data.cameras.authoritative_now&&!$('l-lastknown').checked)continue;const lon=Number(c.longitude),lat=Number(c.latitude);if(inView(lon,lat))consider(project(lon,lat),'camera',c)}if($('l-signs').checked)for(const s of data.message_signs?.signs||[]){if(!data.message_signs.authoritative_now&&!$('l-lastknown').checked)continue;const lon=Number(s.longitude),lat=Number(s.latitude);if(inView(lon,lat))consider(project(lon,lat),'sign',s)}if(!best)return;const[k,o]=best;if(k==='event')showEvent(o);else if(k==='flow')showFlow(o);else if(k==='camera')detail('CAMERA '+o.camera_id,[['source',o.source_family],['road',o.roadway],['direction',o.direction],['location',o.location],['status',o.status],['video',o.video_url||'not exposed']]);else detail('MESSAGE SIGN '+o.sign_id,[['source',o.source_family],['road',o.roadway],['updated',o.updated_at],['messages',o.messages]])}
function showEvent(e){detail((e.event_type||'EVENT').toUpperCase(),[['source family',e.source_family],['provider',e.provider],['class',e.collection_class],['source state',e.source_availability],['record id',e.source_record_id],['upstream id',e.upstream_event_id],['road',e.roadway],['road numbers',e.road_numbers],['from',e.from_location],['to',e.to_location],['direction',e.direction],['county',e.county],['description',e.description],['event detail',e.event_details],['severity',e.severity],['magnitude of delay',e.magnitude_of_delay],['delay seconds',e.delay_seconds],['length meters',e.length_meters],['probability',e.probability_of_occurrence],['time validity',e.time_validity],['full closure',e.full_closure],['lanes',e.lanes_affected],['reported',e.reported_at],['updated',e.updated_at],['provider end-user report count',e.community_report_count],['provider last end-user report',e.community_last_report_at],['provider report age',age(e.community_last_report_age_seconds)]])}
function showFlow(f){detail('TOMTOM FLOW // '+(f.label||f.probe_id),[['source family',f.source_family],['provider',f.provider],['class',f.collection_class],['source state',f.source_availability],['probe id',f.probe_id],['query reference',Number(f.query_latitude).toFixed(5)+', '+Number(f.query_longitude).toFixed(5)],['match method',f.match_method],['matched segment FRC',f.functional_road_class],['current speed mph',f.current_speed_mph],['free-flow speed mph',f.free_flow_speed_mph],['speed / free-flow',Number.isFinite(f.speed_vs_free_flow)?f.speed_vs_free_flow.toFixed(3):null],['current travel sec',f.current_travel_time_seconds],['free-flow travel sec',f.free_flow_travel_time_seconds],['travel-time delta sec',f.travel_time_delta_seconds],['confidence',f.confidence],['road closure',f.road_closure],['OpenLR',f.openlr],['identity note','query label is a reference only; TomTom matched nearest road fragment geometry is the observed segment']])}
function populateViewportIntel(events){const el=$('viewport-intel');if(!el)return;el.replaceChildren();const visible=events||[],closures=visible.filter(e=>e.full_closure===true).length,delayed=visible.filter(e=>Number(e.delay_seconds)>=60).length,crashes=visible.filter(e=>eventKind(e)==='crash').length,hazards=visible.filter(e=>eventKind(e)==='hazard').length,sources=new Set(visible.map(e=>e.source_family).filter(Boolean));const flow=(data.flow?.probes||[]).filter(f=>(f.source_authoritative_now||$('l-lastknown').checked)&&geographicPoints(f.geometry).some(p=>inView(p[0],p[1]))),slow=flow.filter(f=>Number.isFinite(Number(f.speed_vs_free_flow))&&Number(f.speed_vs_free_flow)<.85).length;const grid=document.createElement('div');grid.className='intel-grid';for(const[label,value]of[['EVIDENCE',visible.length],['FULL CLOSE',closures],['DELAYED',delayed],['CRASH',crashes],['HAZARD',hazards],['FLOW SLOW',slow+'/'+flow.length]]){const m=document.createElement('div');m.className='intel-metric';const l=document.createElement('span');l.className='label';l.textContent=label;const n=document.createElement('span');n.className='n';n.textContent=String(value);m.append(l,n);grid.appendChild(m)}el.appendChild(grid);const note=document.createElement('div');note.className='intel-note';note.textContent='SOURCE FAMILIES '+sources.size+' // '+(data.summary?.correlation_mode||'correlation unavailable')+' // attention order is rule-based presentation, not confidence or independent corroboration';el.appendChild(note);const notable=[...visible].sort((a,b)=>eventAttention(b)-eventAttention(a)||String(b.updated_at||b.reported_at||'').localeCompare(String(a.updated_at||a.reported_at||''))).slice(0,8);if(notable.length){const list=document.createElement('div');list.className='intel-list';for(const e of notable){const r=document.createElement('div');r.className='intel-row';r.textContent=eventTitle(e);r.addEventListener('click',()=>{focusEvent(e);showEvent(e)});list.appendChild(r)}el.appendChild(list)}}
function sourceRow(name,s){const d=document.createElement('div');d.className='source';const o=s?.observation||{};const head=document.createElement('div');head.className='source-head';const a=document.createElement('span');a.textContent=name;const b=document.createElement('span');b.textContent=String(o.availability||'unavailable').toUpperCase();b.className=String(o.availability||'unavailable').toLowerCase();head.append(a,b);const sub=document.createElement('div');sub.className='source-sub';let counts;if(Number.isFinite(s?.successful_probe_count)||Number.isFinite(s?.configured_probe_count))counts='probes '+(s?.successful_probe_count??'--')+' / '+(s?.configured_probe_count??'--');else counts='local '+(s?.local_record_count??'--')+' / source '+(s?.source_record_count??'--');sub.textContent=(s?.provider||'--')+' // '+counts+' // success age '+age(o.last_success_age_seconds)+(o.reasons?.length?' // '+o.reasons.join(' // '):'');d.append(head,sub);return d}
function populate(){if(!data)return;const s=$('sources');s.replaceChildren();const es=data.event_sources||{};s.append(sourceRow('DRIVENC EVENTS',es.drivenc_events),sourceRow('WZDx WORK ZONES',es.wzdx),sourceRow('CMPD CAD',es.cmpd),sourceRow('CHARLOTTE CLOSURES',es.charlotte_closures),sourceRow('TOMTOM INCIDENTS',es.tomtom_incidents),sourceRow('TOMTOM FLOW',data.flow),sourceRow('DRIVENC CAMERAS',data.cameras),sourceRow('MESSAGE SIGNS',data.message_signs));const gaps=$('gaps');gaps.replaceChildren();for(const d of data.summary?.degrading_sources||[]){const x=document.createElement('div');x.className='gap';const why=(d.reasons||[]).join(' // ');x.textContent='DEGRADING SOURCE: '+d.label+' // '+String(d.availability||'unavailable').toUpperCase()+(why?' // '+why:'');gaps.appendChild(x)}for(const g of data.summary?.collection_gaps||[]){const x=document.createElement('div');x.className='gap';x.textContent=g;gaps.appendChild(x)}const list=$('event-list');list.replaceChildren();let events=(data.events||[]).filter(e=>visibleSource(e)&&layerEnabled(e.source_key));if($('l-viewport-list').checked&&geoView)events=events.filter(eventInView);populateViewportIntel(events);events.sort((a,b)=>eventAttention(b)-eventAttention(a)||String(b.updated_at||b.reported_at||b.start_at||'').localeCompare(String(a.updated_at||a.reported_at||a.start_at||'')));for(const e of events.slice(0,200)){const r=document.createElement('div');r.className='event-row';const h=document.createElement('div');h.className='headline';for(const[label,klass]of eventBadges(e)){const b=document.createElement('span');b.className='badge '+klass;b.textContent=label;h.appendChild(b)}const title=document.createElement('span');title.textContent=eventTitle(e);h.appendChild(title);const m=document.createElement('div');m.className='meta';const d=Number(e.delay_seconds),bits=[e.source_family,e.collection_class,e.source_availability?.toUpperCase()];if(Number.isFinite(d)&&d>0)bits.push('delay '+Math.round(d)+'s');if(e.direction)bits.push(e.direction);m.textContent=bits.filter(Boolean).join(' // ');r.append(h,m);r.addEventListener('click',()=>{focusEvent(e);showEvent(e)});list.appendChild(r)}}
function zoomAt(delta){if(!mapReady)return;map.easeTo({zoom:Math.max(map.getMinZoom(),Math.min(map.getMaxZoom(),map.getZoom()+delta)),duration:180})}
function focusPoint(lon,lat,miles=5){if(!Number.isFinite(lon)||!Number.isFinite(lat)||!mapReady)return;fitMapBounds(radiusBounds(lat,lon,miles),{padding:70,maxZoom:16,duration:250})}
function focusEvent(e){const pts=geographicPoints(e.geometry);if(!pts.length||!mapReady)return;let w=Infinity,s=Infinity,east=-Infinity,n=-Infinity;for(const[lon,lat]of pts){w=Math.min(w,lon);east=Math.max(east,lon);s=Math.min(s,lat);n=Math.max(n,lat)}if(pts.length===1)return focusPoint(pts[0][0],pts[0][1],5);const lat=(s+n)/2,padLat=Math.max((n-s)*.6,2/69),padLon=Math.max((east-w)*.6,2/(69*Math.max(.2,Math.cos(lat*Math.PI/180))));fitMapBounds([w-padLon,s-padLat,east+padLon,n+padLat],{padding:70,maxZoom:16,duration:250})}
function activeBounds(){if(!data)return null;const pts=[];for(const e of data.events||[]){if(visibleSource(e)&&layerEnabled(e.source_key))pts.push(...geographicPoints(e.geometry))}if($('l-flow').checked)for(const f of data.flow?.probes||[])if(f.source_authoritative_now||$('l-lastknown').checked)pts.push(...geographicPoints(f.geometry));if($('l-cameras').checked&&(data.cameras?.authoritative_now||$('l-lastknown').checked))for(const c of data.cameras?.cameras||[]){const p=[Number(c.longitude),Number(c.latitude)];if(p.every(Number.isFinite))pts.push(p)}if($('l-signs').checked&&(data.message_signs?.authoritative_now||$('l-lastknown').checked))for(const s of data.message_signs?.signs||[]){const p=[Number(s.longitude),Number(s.latitude)];if(p.every(Number.isFinite))pts.push(p)}if(!pts.length)return null;let w=Infinity,s=Infinity,e=-Infinity,n=-Infinity;for(const[lon,lat]of pts){w=Math.min(w,lon);e=Math.max(e,lon);s=Math.min(s,lat);n=Math.max(n,lat)}const lat=(s+n)/2,padLat=Math.max((n-s)*.05,1/69),padLon=Math.max((e-w)*.05,1/(69*Math.max(.2,Math.cos(lat*Math.PI/180))));return[w-padLon,s-padLat,e+padLon,n+padLat]}
async function refresh(){try{const r=await fetch('/api/v1/traffic',{cache:'no-store'});if(!r.ok)throw new Error('HTTP '+r.status);data=await r.json();$('api').textContent='LIVE';$('api').className='value current';state($('picture'),data.summary?.availability);const degrading=Number(data.summary?.degrading_source_count||0);if(String(data.summary?.availability||'').toLowerCase()==='degraded'&&degrading>0)$('picture').textContent='DEGRADED // '+degrading+' SOURCE'+(degrading===1?'':'S');$('kernels').textContent=data.summary?.event_kernels??'--';$('cams').textContent=data.summary?.cameras??'--';$('signs').textContent=data.summary?.active_message_signs??'--';$('flows').textContent=data.summary?.flow_probes??'--';$('generated').textContent=' // served '+data.presentation.generated_at;const mc=data.map_context||{};if(mc.context_url){try{const cr=await fetch(mc.context_url,{cache:'no-store'});if(cr.ok)context=await cr.json()}catch(_e){context=null}}let nextScope=null;if(context?.bounds)nextScope=normalizeBounds([context.bounds.west,context.bounds.south,context.bounds.east,context.bounds.north]);if(!nextScope&&Number.isFinite(data.location.latitude)&&Number.isFinite(data.location.longitude)&&Number.isFinite(data.location.radius_miles))nextScope=radiusBounds(data.location.latitude,data.location.longitude,data.location.radius_miles);if(nextScope){const scopeChanged=!boundsClose(nextScope,scopeBounds);scopeBounds=nextScope;if(mapReady){if(scopeChanged)updateScopeCamera();if(!mapInitialized){const saved=savedCamera();if(saved){map.jumpTo(saved)}else fitMapBounds(scopeBounds,{padding:18});mapInitialized=true}syncGeoView()}}populate();draw()}catch(err){$('api').textContent='UNAVAILABLE';$('api').className='value unavailable';$('map-note').textContent=String(err)}}
for(const id of ['l-events','l-wzdx','l-city','l-cmpd','l-tomtom','l-flow','l-cameras','l-signs','l-lastknown'])$(id).addEventListener('change',()=>{draw();populate()});
$('l-detail-base').addEventListener('change',()=>{applyDetailVisibility();draw()});
$('l-viewport-list').addEventListener('change',populate);
$('scope-view').addEventListener('click',()=>scopeBounds&&fitMapBounds(scopeBounds,{padding:18,duration:250}));
$('local-view').addEventListener('click',()=>{const site=data?.operator_context?.site_anchor;if(site&&Number.isFinite(Number(site.latitude))&&Number.isFinite(Number(site.longitude)))fitMapBounds(radiusBounds(Number(site.latitude),Number(site.longitude),15),{padding:34,duration:250});else if(data)fitMapBounds(radiusBounds(Number(data.location.latitude),Number(data.location.longitude),15),{padding:34,duration:250})});
$('fit-active').addEventListener('click',()=>{const b=activeBounds();if(b)fitMapBounds(b,{padding:42,maxZoom:15,duration:250})});
$('zoom-in').addEventListener('click',()=>zoomAt(1));
$('zoom-out').addEventListener('click',()=>zoomAt(-1));
$('rotate-left').addEventListener('click',()=>mapReady&&map.easeTo({bearing:map.getBearing()-15,duration:180}));
$('rotate-right').addEventListener('click',()=>mapReady&&map.easeTo({bearing:map.getBearing()+15,duration:180}));
$('pitch-up').addEventListener('click',()=>mapReady&&map.easeTo({pitch:Math.min(70,map.getPitch()+10),duration:180}));
$('pitch-down').addEventListener('click',()=>mapReady&&map.easeTo({pitch:Math.max(0,map.getPitch()-10),duration:180}));
$('north-view').addEventListener('click',()=>mapReady&&map.easeTo({bearing:0,pitch:0,duration:220}));
$('map-wrap').addEventListener('wheel',e=>{e.preventDefault();e.stopPropagation()},{passive:false});
$('load-waze').addEventListener('click',()=>{const w=data?.external_visual_sources?.waze;if(!w?.enabled||!w.url){$('waze-disclosure').textContent='Waze external visual source is unavailable in the current traffic projection.';return}const f=document.createElement('iframe');f.src=w.url;f.title='Waze Live Map external crowd traffic view';f.referrerPolicy='strict-origin-when-cross-origin';$('waze-holder').replaceChildren(f);$('waze-disclosure').textContent=w.disclosure});
initMapEngine();refresh();setInterval(refresh,15000);
</script>
</body>
</html>'''
